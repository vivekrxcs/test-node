<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  <h1> May Node and Express be with you. </h1>
  
  <form action="/quotes" method="POST">
    <input type="text" placeholder="name" name="name">
    <input type="text" placeholder="quote" name="quote">
    <button type="submit">Submit</button>
  </form>

<table>
  <tr>
    <th>No</th>
    <th>name</th>
    <th>quote</th>
    <th>Action</th>
  </tr>
{{#each data}}
  <tr>
    <td>{{@key}}</td>
    <td>{{this.name}}</td>
    <td>{{this.quote}}</td>
    <td><a href="{{this._id}}" class="btn button-success">Delete</a></td>
  </tr>
 {{/each}}
 </table>
</body>
</html>